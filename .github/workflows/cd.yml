name: CD

# Delete workflow_dispatch and the placeholder job before modifying this file

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CD not implemented yet"
